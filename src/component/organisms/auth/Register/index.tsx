import {RegisterContainer} from "./style";
import RegisterForm from "../RegisterForm";

function Register() {
  return (
    <RegisterContainer>
      <RegisterForm/>
    </RegisterContainer>
  );
}

export default Register;